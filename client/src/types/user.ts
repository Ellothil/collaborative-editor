/**
 * Represents a user in the collaborative editor.
 *
 * @property {string} name - The user's name.
 * @property {string} color - The user's color.
 */
export type User = {
  name: string;
  color: string;
};
