{
  "name": "collaborative-editor",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "pnpm --recursive --parallel dev",
    "build": "pnpm --recursive build",
    "pre-commit": "pnpm ultracite fix"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.0",
    "husky": "^9.1.7",
    "ultracite": "6.0.1"
  }
}
